import { FormEvent, useEffect, useState } from 'react';
import { api } from '../../lib/api';
import { BriefcaseBusiness, Plus, UserX } from 'lucide-react';

type Employee = { _id: string; firstName: string; lastName: string; email: string; role: string; isActive: boolean };
const roles = ['admin', 'accountant', 'seller', 'hr', 'stock_manager', 'project_manager', 'employee'];
export default function EmployeesPage() {
  const [employees, setEmployees] = useState<Employee[]>([]); const [open, setOpen] = useState(false); const [error, setError] = useState(''); const [form, setForm] = useState({ firstName: '', lastName: '', email: '', password: '', role: 'employee' });
  const load = () => api<Employee[]>('/employees').then(setEmployees).catch(e => setError(e.message)); useEffect(() => { load(); }, []);
  async function submit(e: FormEvent) { e.preventDefault(); try { await api('/employees', { method: 'POST', body: JSON.stringify(form) }); setOpen(false); setForm({ firstName: '', lastName: '', email: '', password: '', role: 'employee' }); load(); } catch (e) { setError(e instanceof Error ? e.message : 'Erreur'); } }
  async function deactivate(id: string) { if (!confirm('Désactiver cet employé ?')) return; await api(`/employees/${id}`, { method: 'DELETE' }); load(); }
  return <div className="space-y-6"><div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-sm font-semibold uppercase tracking-widest text-emerald-600">Équipe</p><h1 className="text-3xl font-black">Employés</h1><p className="mt-1 text-slate-500">Gérez les accès et les responsabilités de votre équipe.</p></div><button onClick={() => setOpen(!open)} className="inline-flex items-center justify-center gap-2 rounded-xl bg-emerald-600 px-4 py-2.5 text-sm font-bold text-white"><Plus size={17} /> Inviter un employé</button></div>{error && <div className="rounded-xl bg-red-50 px-4 py-3 text-sm text-red-700">{error}</div>}{open && <form onSubmit={submit} className="grid gap-3 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm sm:grid-cols-2 dark:border-slate-800 dark:bg-slate-900"><input required placeholder="Prénom" value={form.firstName} onChange={e => setForm({ ...form, firstName: e.target.value })} className="field" /><input required placeholder="Nom" value={form.lastName} onChange={e => setForm({ ...form, lastName: e.target.value })} className="field" /><input required type="email" placeholder="Email professionnel" value={form.email} onChange={e => setForm({ ...form, email: e.target.value })} className="field" /><input required minLength={8} type="password" placeholder="Mot de passe temporaire" value={form.password} onChange={e => setForm({ ...form, password: e.target.value })} className="field" /><select value={form.role} onChange={e => setForm({ ...form, role: e.target.value })} className="field">{roles.map(role => <option key={role}>{role}</option>)}</select><button className="rounded-xl bg-slate-900 px-4 py-2 text-sm font-bold text-white">Créer le compte</button></form>}<div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{employees.map(employee => <div key={employee._id} className="rounded-2xl border border-slate-200 bg-white p-5 dark:border-slate-800 dark:bg-slate-900"><div className="flex items-start justify-between"><div className="flex h-11 w-11 items-center justify-center rounded-xl bg-emerald-100 font-bold text-emerald-700">{employee.firstName[0]}{employee.lastName[0]}</div><button title="Désactiver" onClick={() => deactivate(employee._id)} className="text-slate-400 hover:text-red-600"><UserX size={17} /></button></div><h2 className="mt-4 font-bold">{employee.firstName} {employee.lastName}</h2><p className="text-sm text-slate-500">{employee.email}</p><div className="mt-4 flex items-center justify-between"><span className="rounded-full bg-slate-100 px-2.5 py-1 text-xs font-semibold dark:bg-slate-800">{employee.role}</span><span className={employee.isActive ? 'text-xs font-semibold text-emerald-600' : 'text-xs text-slate-400'}>{employee.isActive ? 'Actif' : 'Inactif'}</span></div></div>)}{employees.length === 0 && <div className="col-span-full rounded-2xl border border-dashed border-slate-300 py-16 text-center text-slate-400"><BriefcaseBusiness className="mx-auto mb-2" />Aucun employé</div>}</div></div>;
}
